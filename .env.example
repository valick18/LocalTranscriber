# Ollama Configuration (for Local Chat)
# Ensure you have installed Ollama and pulled this model (e.g. `ollama pull llama3`)
OLLAMA_MODEL=llama3

# Security: Secret Key for API Access (REQUIRED for security)
# Generate a strong key (e.g., using `openssl rand -hex 32` or a password generator)
API_SECRET_KEY=your_super_secret_api_key_here

# Whisper Model Configuration
# Options: tiny, base, small, medium, large-v3
# large-v3 requires ~10GB RAM. base requires ~500MB.
WHISPER_MODEL_NAME=large-v3
